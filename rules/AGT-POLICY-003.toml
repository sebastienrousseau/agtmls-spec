# SPDX-FileCopyrightText: 2026 Sebastien Rousseau
# SPDX-License-Identifier: Apache-2.0 OR MIT

id = "AGT-POLICY-003"
category = "policy_honesty"
severity = "high"
title = "safety_policy is not an object"
kind = "structural"

# Structural rules are implemented in code rather than as a pattern, but
# are declared here so the rule set can be enumerated from data alone.
description = '''The declared policy is structurally invalid and cannot be evaluated.'''
